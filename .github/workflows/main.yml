name: CI

on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
     - name: build
       run: |
        pwd
        ls
        mkdir builds
        cd ./builds
        cmake ..
        cmake --build .
     - name: test
       run: |
        ./builds/test/Tiled_Matrix_googletest
